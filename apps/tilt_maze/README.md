# tilt_maze — 倾斜迷宫(as-built)

> 本文件是 **tilt_maze 应用的竣工记录**(原单-app CLAUDE.md §20.1–20.12,2026-07 迁出;该文档已重构为平台手册)。
> 设计规格(为什么这么设计)见同目录 `SPEC.md`;跨应用平台事实(休眠/电源/单元接入)见根平台手册 `CLAUDE.md`(§7 电源·休眠、§10 单元接入)。
> **占 ota_0(0x210000)**,外设 = IMU MPU6886(内部 I2C @0x68,由 Bottom2 提供)。
> 烧录:`tools/flash_one.sh tilt_maze`。

## 状态总览

**7 个里程碑(M0–M7)核心功能全部实现,build 干净通过。** 可玩闭环:
**倾斜 → 滚球 → 撞墙四通道反馈 → 到家全屏庆祝 → 关卡循环 → idle 打盹 → 动一下唤醒 → 底部长按 3s 进家长菜单。**

实机验证(设备 CP2104 → Core2 v1.0 + Bottom2):
- ✅ **已实机确认**:M0(I2C 扫到 0x68、WHO_AM_I=0x19、屏/声/灯/震动全 OK)、M1(三轴打印)、M2(球随倾斜滚、重而稳)、M3(滑行碰撞、到达)、M4(四通道反馈)、M5(状态机闭环 + 关卡 + 难度档)、idle 打盹(实测 12s 触发)。
- ⏳ **待看屏/上手确认**:唤醒交互、家长菜单 UI、M7 视觉项(瞳孔朝向、挤压拉伸、收集星吸收动画、接近家脉动加快)。
- 🔴 **遗留问题(待排查)**:家长菜单难以进入(底部长按 3s 判定,用户实测反馈)。

## 定案数值(全部在 `main/tuning.h`)

| 项 | 定案值 | 备注 |
|---|---|---|
| 轴映射 | `TILT_INVERT_X=1, TILT_INVERT_Y=0, TILT_SWAP_XY=0` | 实机标定,用户确认(§8.3) |
| 手感 | `GAIN=1800` / `DAMPING=0.90` / `VEL_MAX=220` / `DEADZONE=0.06` / `TILT_ALPHA=0.25` | 重而稳、不飘、封顶 |
| 球/家 | `BALL_R=7` / `GOAL_R=13` / `STAR_R=7` / `WALL_RESTITUTION=0.2` / `BUMP_MIN_SPEED=35` | 16×12 网格缩放后(见下) |
| 步长 | `PHYS_DT=1/60` / `PHYS_PERIOD_MS=16`(`FREERTOS_HZ=1000`) | 固定 dt 60Hz |
| 状态机 | `ATTRACT_TILT_THRESH=0.22` / `WIN_HOLD_MS=1800` | 免校准,`CALIB_*` 已删 |
| idle/省电 | `PLAY_BRIGHTNESS=60` / `IDLE_BRIGHTNESS=10` / `IDLE_TIMEOUT_MS=12000` / `IDLE_WAKE_THRESH=0.12` | 打盹(机制见根 `CLAUDE.md` §7.1) |
| 深度省电 | `DEEP_IDLE_TIMEOUT_MS=60000` / `DEEP_IDLE_POLL_MS=120` / `WAKE_DEBOUNCE_FRAMES=3` | 断 DCDC3+切 5V+去抖唤醒 |
| 灯带/家长 | 灯带亮度上限 `48`(`ledstrip_fx_set_max_brightness`) / `PARENT_LONGPRESS_MS=3000` | |

## main/ 结构

```
main/  app_main.c(core2_board_init 一键 bring-up + 起状态机)
       game_state.c(60Hz 任务 + 状态机 ATTRACT/PLAY/WIN;省电托管给 core2_sleep)
       physics.c · maze.c(关卡 + BFS 校验 + 滑行碰撞)· render.c(三层 + 特效)
       feedback.c(四通道编排器)· parent_menu.c · tuning.h
```
- **任务/队列**:game_task(60Hz)读 IMU→物理→碰撞→渲染→发事件;feedback/audio/haptics/ledstrip 各自事件驱动消费队列,**不阻塞 game_task**(符合 §3.2)。
- **免校准**:无零点/校准概念,`physics_set_position` 只复位 pos/vel;低通滤波首帧样本自预热。
- **打盹/唤醒**:游戏侧每帧 `core2_sleep_feed()` 喂加速度即可;机制/踩坑见根 `CLAUDE.md` §7(电源/休眠/背光)。

## 落地实现 vs 规格的差异(刻意选择,均在帧预算/纪律内)

| 项 | 规格(§) | 实际落地 | 原因 |
|---|---|---|---|
| 音效素材 | 预制 PCM 片段(root §5.1) | **程序化合成**(sine + 包络,首尾淡入淡出);codec **整局保持 open**,空闲写静音 | 无需打包素材;root §5.1 防爆音纪律照样满足 |
| 美术 | 烘焙精灵图(root §6.4 / SPEC §18) | **程序化色块/圆角块/圆形球**(LVGL 对象,在 root §6.2 帧预算内) | 先保证手感与帧率;精灵图是后续打磨项 |
| 收集星形状 | 五角星双色(§18.3) | **圆形占位**(吸收动画=放大淡出 OK) | 五角星需精灵/canvas,列为可选 |
| 家长菜单文案 | 中文图标+滑块(root §8) | **英文标签 + LVGL 符号** | LVGL 默认字体无 CJK 字形;要中文需配 CJK 字体 |
| 吉祥物换帽/待机微动 | §18.4/§18.5 | **未做**(同一张程序化脸) | 锦上添花;帧预算优先给球 |
| idle 省电 | 可深度休眠(root §7) | **两级**:打盹(12s)降亮 60%→10% + 灯带慢呼吸;深度省电(再 60s)真黑屏(断 DCDC3)+ 灯带熄 + 切 5V + IMU 降频轮询(120ms) | 关背光+切 5V 是最大两个耗电点 |
| 眨眼 | 静止时眨眼(§5.1) | **未做**(瞳孔朝速度方向已做) | 可选打磨 |
| 校准 | §8.2 进关采样零点 | **已整体删除**:绝对零点免校准,ATTRACT 倾斜触发直接进 PLAY | "平"=与地面平、规则永远一致;端平本身是要教的东西(见下) |

## 绝对零点,免校准(2026-07-02 设计定案,推翻 §8.2)

**定案**:删除整个校准机制。"平"= 屏幕平面与地面平(重力绝对参考),规则永远一致。

**设计依据(用户拍板)**:游戏目的就是让孩子学会控制设备——把设备端平正是要教的东西,物理滚珠迷宫玩具就是这个范式(它们从不"校准"到握姿)。原 §8.2 姿态校准替孩子做了他能自己学会的动作,代价是:①同一物理倾斜在不同会话里作用不同(违背"因果一致"第一性原则);②引入隐藏的、每次都变的参考系;③滋生零点污染 bug。斜着拿球就往下滚 → 孩子自己调整设备对抗,这就是游戏本身。

**技术前提(已实机验证)**:板子加速度计偏置须小于死区 0.06g。本机平放实测 `x=-0.023 y=-0.007 z=1.065,漂移 0.001g` —— x/y 偏置不到死区一半,余量充足;z 偏差不影响玩法(只用 x/y)。

**落地变更**:
- 删 `ST_CALIBRATE` 态与 `calibrate_tick`、`physics_calibrate()`、`physics_t.zero`;ATTRACT 倾斜触发**直接进 PLAY**;
- 删家长菜单"Recal"按钮与 `game_state_request_recalibrate()`(用户明确不要修正入口);
- 删 tuning:`CALIB_*`、`IDLE_STILL_THRESH`;
- 低通滤波改为首帧样本自预热;
- **保留**:减除式死区(见下)、速度封顶、轴映射。
- **已否决**:静止自回零(偷偷移动"平"违背一致性原则)、NVS/菜单偏置修正(本机余量充足)。

**状态**:✅ 已实机验证(2026-07-02 烧录确认)。手感若偏慢,调 `TILT_GAIN` 1800→~2000,勿动死区;斜着拿球向下滚属预期。

### 根因记录:IMU"平放单向慢爬"(2026-07-02,已随免校准修复)

**症状**:有时平放机身,球仍以 ~20px/s 匀速朝固定方向爬。**根因不是 IMU 硬件**:
1. **校准零点被"开始手势"污染(主因,免校准后已消失)**:旧 `calibrate_tick` 从进状态第 1 帧无条件采样,离开 ATTRACT 的倾斜手势把零点拖偏 ≈0.06g(恰好一个死区)。
2. **硬死区悬崖(放大器,已改减除式)**:残余一旦过线(0.06g)就吃全额增益 → ~21px/s 匀速爬。

**保留的修复 · 减除式死区**:只把超出死区的部分交给增益(`(tmag−死区)/tmag` 缩放),消除"过线即全额恒力"的悬崖——零点即使略有污染,残余小力也会被阻尼压住。**注意**:减除式让小倾斜有效力整体减小 0.06g,手感略"重";偏慢时微调 `TILT_GAIN`,勿动死区本身。

## 关卡库:16×12 分叉迷宫(2026-07-02 定案,12 关)

> 关卡演进史(4 关 → 8 关随机洗牌袋 → 12 关无岔路 → 16×12 分叉迷宫)见 git log;**以下为当前形态**。

- **动机**:用户实测——孩子已完全掌握控球,无岔路地图太简单。8×6 网格塞不下岔路,**分辨率翻倍:`MAZE_COLS/ROWS = 16×12,MAZE_CELL = 20px`**(仍恰好铺满 320×240,无偏移)。难度来源从"拐弯数"升级为:**分叉/死胡同(走错要折返)、环路(两条都通,选路非对错)、1 格窄走廊(20px)精细控制**。零失败原则不变。
- **随格子缩放的常量**:`BALL_R 12→7`、`GOAL_R 20→13`、`STAR_R=7`(tuning.h,渲染与拾取共用);render.c 眼睛/瞳孔等比缩小、家圆盘 `2×GOAL_R`、地砖圆角 4、瞳孔偏移 1px。**物理手感参数一律未动。**
- **12 关结构**:L1~L4 保持 **2 格宽走廊**(=40px 旧手感):L1/L2 无岔路(Easy 档 = 前 2 关、开局固定 L1),L3 宽蛇形,L4 引入第一个死胡同支路;L5 起 1 格窄走廊真迷宫——L7 梳齿(5 假齿)、L8 回字环、L9/L11/L12 含环路的满盘迷宫、L10 全宽房间+窄门回旋。
- **星规则(刻意)**:星不再必在最短路上,**多数藏进支路/死胡同底**引诱探索(星非过关必需,死胡同因此变成"寻宝"而非挫败)。
- **过关切关 = 洗牌袋**(`game_state.c` next_level_from_bag):Fisher–Yates 洗出一轮,一轮内每关恰好出现一次,打完重洗;开局固定 L1 且计入第一轮洗牌袋(`first_level_from_bag`)。难度档:Normal = 全部(走 `maze_level_count()`),Easy = 前 2 关。
- **设计纪律(离线脚本逐关验证,直接解析 maze.c)**:①BFS 连通 + 全路面格可达(无孤岛);②S/H/* 网格字符与 start/home/stars 字段一致;③脚本另报告死胡同/岔口/环路数与最短路长/弯数。
- ✅ build + flash 通过,开机日志干净;⏳ 待实机确认:窄走廊手感(若太难压 `VEL_MAX` 220→~160,勿改地图)、20px 门洞通过性、新尺寸下视觉。

## 剩余可选打磨(非阻塞,做不做都已是完整可玩成品)

1. 烘焙美术精灵图 + 各世界给圆圆换帽(§18.4)。
2. §18.5 待机微动(花摇/窄浪带/星眨/糖霜滴)——严格压在 root §6.2 帧预算内。
3. 五角星形收集物;静止眨眼;触屏直接开始(现仅倾斜触发)。
4. 家长设置 **NVS 持久化**(现重启回默认)。
5. 中文化家长菜单(配 CJK 字体)。
6. 若要再省电可上真·light-sleep(需 MPU6886 INT → GPIO 唤醒,复杂度高)。
