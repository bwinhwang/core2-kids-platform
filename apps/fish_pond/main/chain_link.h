// chain_link —— Chain 绑定层(SPEC.md §8):joy 回中修复 + enc 增量;attach/重试/拔线
// 判定形状抄 chain_lab;起 30Hz game_task + core2_sleep 集成(kick = 任一输入 activity)。
#pragma once

#ifdef __cplusplus
extern "C" {
#endif

/** @brief 建整个池塘场景(pond/boat/fish/bucket + 无字提示卡)、起 Chain 传输 + 30Hz 任务。 */
void chain_link_start(void);

#ifdef __cplusplus
}
#endif
